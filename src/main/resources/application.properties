server.port=8080
spring.application.name=be-project

# JWT secret (override in production)
app.jwt.secret=ChangeThisToAStrongSecretKeyOfAtLeast32Bytes

# SQLite datasource
spring.datasource.url=jdbc:sqlite:auth.db
spring.datasource.driver-class-name=org.sqlite.JDBC
spring.datasource.initialization-mode=never
